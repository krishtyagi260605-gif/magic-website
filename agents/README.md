# Agents Workspace

Use this workspace for production workflows, evaluators, retrieval experiments, and domain-specific agent packs.

Suggested next additions:

- `finance_agent.py` for reconciliations and reporting automation
- `healthcare_agent.py` for intake triage and retrieval-safe workflows
- `supply_chain_agent.py` for exception monitoring and supplier intelligence
- `evals/` for regression tests across prompts, tools, and retrieval quality

